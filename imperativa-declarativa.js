// Função que eleva o número ao quadrado.
// Imperativa: Faça da seguinte forma. (DAR ORDENS)

let number = 2;

function square() {
  return number * number;
}

number = square();

// Declarativa: O que fazer e não como fazer.
const square = (n) => n * m;
